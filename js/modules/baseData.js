'use strict';

const input = document.getElementById('input-field');
const btn = document.querySelector('.add-form-button');
const textarea = document.getElementById('textarea-field');
export const list_comments = document.getElementById('list-comments');
export const likeButton = document.querySelectorAll(".like-button");

export {input,btn,textarea};